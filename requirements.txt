# This file contains the dependencies for the SaaS project.
# Each line specifies a package required for the project to run.
# Use 'pip install -r requirements.txt' to install all dependencies.

Flask
SQLAlchemy
requests
python-dotenv
gunicorn
passlib
Flask-Migrate
Flask-SQLAlchemy
psycopg2-binary
